# Python dependencies for the performance alert and PR/CI pipeline.
#
# Used by:
#   - check_perf_changes.py     (alerts on >20% regressions)
#   - push_to_github.py         (publishes results to this repo)
#   - test_perf_alerts.py       (unit tests for the alert path)
#
# NOT used here (run on Tenstorrent hardware against tt-metal's own env):
#   - perf_measurement_script.py / test_eltwise_operations.py
#     These require torch, numpy, scipy, ttnn, pytest and are not pinned here.

requests>=2.31,<3
