## Summary

<!-- What does this PR do and why? Keep it short. -->

## Changes

<!-- Bullet list of the user-visible or behavioural changes. -->
-

## Test plan

<!-- How did you verify this? Tick what applies and add detail. -->
- [ ] `npm run build` succeeds
- [ ] Manually verified the dashboard renders (`npm run dev` or `npm run preview`)
- [ ] Worker `/api/*` + alert path tested (`npx wrangler dev --local`) — if the Worker was touched

## Screenshots

<!-- For UI changes, attach before/after screenshots. -->

## Notes for reviewers

<!-- Anything reviewers should pay extra attention to, follow-ups, etc. -->
